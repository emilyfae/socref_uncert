d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "novel"]
d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "mutual"]
test <- t.test(
d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))
View(d_ttest)
test <- t.test(
d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
test <- t.test(
d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "familiar"], d_ttest$num_looks[d_ttest$age_years == 3 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
test <- t.test(
d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
test <- t.test(
d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "familiar"], d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
test <- t.test(
d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
test <- t.test(
d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "familiar"], d_ttest$num_looks[d_ttest$age_years == 4 & d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
test <- t.test(
d_ttest$num_looks[d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
test <- t.test(
d_ttest$num_looks[d_ttest$trial_type == "familiar"], d_ttest$num_looks[d_ttest$trial_type == "mutual"],
paired = TRUE
)
test
View(d_ttest)
#exploratory t-tests - in the no-gaze condition, do children show the following pattern of looking: NN > FN > FF?
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))
test <- t.test(
d_ttest$num_looks[d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$trial_type == "mutual"],
paired = TRUE
)
test <- t.test(
d_ttest$num_looks[d_ttest$trial_type == "familiar"], d_ttest$num_looks[d_ttest$trial_type == "mutual"],
paired = TRUE
)
test <- t.test(
d_ttest$num_looks[d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$trial_type == "mutual"],
paired = TRUE
)
test <- t.test(
d_ttest$num_looks[d_ttest$trial_type == "novel"], d_ttest$num_looks[d_ttest$trial_type == "mutual"],
paired = TRUE
)
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))
View(d_ttest)
d_ttest$num_looks[d_ttest$trial_type == "novel"]
d_ttest$num_looks[d_ttest$trial_type == "mutual"]
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type)
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type, num_looks, "looks")
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type, num_looks)
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type, num_looks, "looks")
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type, num_looks)
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type, num_looks)%>%
filter(!is.na(novel))
warnings()
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type)%>%
filter(!is.na(novel))
test <- t.test(
d_ttest$novel, d_ttest$mutual,
paired = TRUE
)
test
test <- t.test(
d_ttest$familiar, d_ttest$mutual,
paired = TRUE
)
test
test <- t.test(
d_ttest$novel, d_ttest$mutual,
paired = TRUE
)
test
ttest_FN_NN <- t.test(
d_ttest$novel, d_ttest$mutual,
paired = TRUE
)
ttest_FN_FF <- t.test(
d_ttest$familiar, d_ttest$mutual,
paired = TRUE
)
ttest_FN_NN
ttest_FN_FF
ttest_FN_NN <- t.test(
d_ttest$novel[d_ttest$age_years == 3], d_ttest$mutual[d_ttest$age_years == 3],
paired = TRUE
)
ttest_FN_NN
ttest_FN_NN <- t.test(
d_ttest$familiar[d_ttest$age_years == 3], d_ttest$mutual[d_ttest$age_years == 3],
paired = TRUE
)
ttest_FN_NN
#exploratory t-tests - in the no-gaze condition, do children show the following pattern of looking: NN > FN > FF?
d_ttest <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")%>%
group_by(SID, trial_type, age_years)%>%
summarise(num_looks = mean(num_looks))%>%
spread(trial_type, num_looks)%>%
filter(!is.na(novel))
ttest_FN_NN <- t.test(
d_ttest$novel, d_ttest$mutual,
paired = TRUE
)
ttest_FN_FF <- t.test(
d_ttest$familiar, d_ttest$mutual,
paired = TRUE
)
#3yos
ttest_FN_NN_3 <- t.test(
d_ttest$novel[d_ttest$age_years == 3], d_ttest$mutual[d_ttest$age_years == 3],
paired = TRUE
)
ttest_FN_FF_3 <- t.test(
d_ttest$familiar[d_ttest$age_years == 3], d_ttest$mutual[d_ttest$age_years == 3],
paired = TRUE
)
#4yos
ttest_FN_FF_4 <- t.test(
d_ttest$novel[d_ttest$age_years == 4], d_ttest$mutual[d_ttest$age_years == 4],
paired = TRUE
)
ttest_FN_FF_4 <- t.test(
d_ttest$novel[d_ttest$age_years == 4], d_ttest$mutual[d_ttest$age_years == 4],
paired = TRUE
)
ttest_FN_FF_4
ttest_FN_FF_4
ttest_FN_FF_3
ttest_FN_NN_3
ttest_FN_FF_3
d_lmer_lim <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")
View(d)
d_lmer_lim <- filter(d, phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze")
styler:::style_selection()
d_lmer_lim <- filter(
d,
phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze"
)
d_lmer_lim <- filter(
d,
phase_name == "response",
Gaze == "no_gaze"
)
styler:::style_selection()
d_lmer_lim <- d %>%
filter(
phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze"
)
library(papaja)
library(tidyverse)
library(png)
library(grid)
library(ggplot2)
library(xtable)
library(langcog)
library(ggplot2)
library(rjson)
library(stringr)
library(lme4)
library(knitr)
library(markdown)
library(lmerTest)
library(ggthemes)
library(psych)
library(broman)
library(kmr)
library(magrittr)
library(stargazer)
d_lmer_lim <- d %>%
filter(
phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze"
)
styler:::style_selection()
d_lmer_lim <- filter(
d,
phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze"
)
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c +
(trial_type | SID),
data = lmer_data))
g_lm <- summary(lmer(num_looks ~ trial_type * age_c +
(1 | SID),
data = lmer_data))
g_lm
ttest_FN_FF_4
ttest_FN_FF_4
ttest_FN_FF_3
ttest_FN_NN_3
ttest_FN_NN
ttest_FN_FF
View(d_lmer_lim)
d_lmer_lim$trial_type <- relevel(d_lmer_lim$trial_type, ref = "mutual")
g_lm <- summary(lmer(num_looks ~ trial_type * age_c +
(1 | SID),
data = lmer_data))
g_lm
d_lmer_ng <- filter(
d,
phase_name == "response",
exclude == FALSE,
Gaze == "no_gaze"
)
d_lmer_g <- filter(
d,
phase_name == "response",
exclude == FALSE,
Gaze == "gaze"
)
d_lmer_lim$trial_type <- relevel(d_lmer_lim$trial_type, ref = "mutual")
g_lm <- summary(lmer(num_looks ~ trial_type * age_c +
(1 | SID),
data = d_lmer_ng))
d_lmer_ng <- filter(
lmer_data,
phase_name == "response",
Gaze == "no_gaze"
)
d_lmer_g <- filter(
lmer_data,
phase_name == "response",
Gaze == "gaze"
)
d_lmer_ng$trial_type <- relevel(d_lmer_ng$trial_type, ref = "mutual")
g_lm <- summary(lmer(num_looks ~ trial_type * age_c +
(1 | SID),
data = d_lmer_ng))
g_lm
d_lmer_g$trial_type <- relevel(d_lmer_g$trial_type, ref = "mutual")
g_lm <- summary(lmer(num_looks ~ trial_type * age_c +
(1 | SID),
data = d_lmer_g))
g_lm
d_lmer_ng$trial_type <- relevel(d_lmer_ng$trial_type, ref = "mutual")
g_lm <- summary(lmer(num_looks ~ trial_type * age_c +
(1 | SID),
data = d_lmer_ng))
g_lm
lmer_data$phase_name <- relevel(lmer_data$phase_name, ref = "slide")
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c * gaze * phase_name +
(trial_type | SID),
data = lmer_data))
g_lm
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c * gaze * phase_name +
(trial_type | SID),
data = filter(lmer_data, phase = "response")))
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c * gaze * phase_name +
(trial_type | SID),
data = filter(lmer_data, phase == "response")))
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c * gaze * phase_name +
(trial_type | SID),
data = lmer_data))
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c * gaze * phase_name +
(trial_type | SID),
data = filter(lmer_data, phase == "response")))
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase == "response")))
filter(lmer_data, phase == "response")
View(lmer_data)
g_lm <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "response")))
g_lm
g_lm_r <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "response")))
g_lm_r
g_lm_r
g_lm_l <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "label")))
g_lm_s <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "slide")))
g_lm_p <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "planing")))
g_lm_r <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "response")))
g_lm_l <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1 | SID),
data = filter(lmer_data, phase_name == "label")))
g_lm_s <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1 | SID),
data = filter(lmer_data, phase_name == "slide")))
g_lm_p <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1 | SID),
data = filter(lmer_data, phase_name == "planing")))
g_lm_l <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "label")))
g_lm_s <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "slide")))
g_lm_s <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1| SID),
data = filter(lmer_data, phase_name == "slide")))
g_lm_p <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "planning")))
g_lm_r <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "response")))
g_lm_l
g_lm_s
g_lm_p
g_lm_r
g_lm_l <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "label")))
g_lm_s <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1| SID),
data = filter(lmer_data, phase_name == "slide")))
g_lm_p <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "planning")))
g_lm_r <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "response")))
g_lm_l
g_lm_s
lmer_data$trial_type <- relevel(lmer_data$trial_type, ref = "mutual")
g_lm_l <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "label")))
g_lm_l <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "label")))
g_lm_s <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1| SID),
data = filter(lmer_data, phase_name == "slide")))
g_lm_p <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "planning")))
g_lm_r <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(trial_type | SID),
data = filter(lmer_data, phase_name == "response")))
g_lm_l <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1 | SID),
data = filter(lmer_data, phase_name == "label")))
g_lm_s <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1| SID),
data = filter(lmer_data, phase_name == "slide")))
g_lm_p <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1 | SID),
data = filter(lmer_data, phase_name == "planning")))
g_lm_r <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1| SID),
data = filter(lmer_data, phase_name == "response")))
g_lm_l
g_lm_s
g_lm_p
g_lm_r
g_lm_l
g_lm_s
g_lm_p
g_lm_r
lmer_data$trial_type <- relevel(lmer_data$trial_type, ref = "familiar")
g_lm_r <- summary(lmer(num_looks ~ trial_type *  age_c * gaze  +
(1| SID),
data = filter(lmer_data, phase_name == "response")))
g_lm_r
X = c(2,4,15,20)
X = c(2,4,15,20)
X = c(2,4,15,20)
Z = c(0,0,1,1)
mm1 = lm(X~Z)
mm1 = lm(X~Z)
res1 = mm1$residuals
mm1
summary(mm1)
mm2 = lm(Y~Z)
Y = c(1,2,3,4)
mm2 = lm(Y~Z)
res2 = mm2$residuals
cor(res1,res2)
cor(X,Y)
load("soc_ref_e1.RData")
hist_e1 <- d %>%
select(phase_name, num_looks)
load("soc_ref_e2.RData")
hist_e2 <- d %>%
select(phase_name, num_looks)
View(hist_e1)
load("soc_ref_e1.RData")
hist_e1 <- d %>%
select(phase_name, num_looks)%>%
mutate(Experiment = "1")
load("soc_ref_e2.RData")
hist_e2 <- d %>%
select(phase_name, num_looks)%>%
mutate(Experiment = "2")
hist<- hist_e1%>%
bind_rows(hist_e2)
View(hist)
hist$phase_name <- factor(hist$phase_name,
levels = c("label", "slide", "planning", "response"),
labels = c("Label", "Slide", "Planning", "Response"))
ggplot(hist, aes(num_looks, fill = num_looks)) +
geom_histogram(stat = "count") +
facet_grid(Experiment~phase_name) +
ggthemes::theme_few() +
labs(x = "Number of looks", y = "Count") +
scale_fill_discrete(name="Trial type")
#histogram for number of looks by phase.
load("soc_ref_e1.RData")
hist_e1 <- d %>%
select(phase_name, num_looks)%>%
mutate(Experiment = "Experiment 1")
load("soc_ref_e2.RData")
hist_e2 <- d %>%
select(phase_name, num_looks)%>%
mutate(Experiment = "Experiment 2")
hist<- hist_e1%>%
bind_rows(hist_e2)
hist$phase_name <- factor(hist$phase_name,
levels = c("label", "slide", "planning", "response"),
labels = c("Label", "Slide", "Planning", "Response"))
ggplot(hist, aes(num_looks, fill = num_looks)) +
geom_histogram(stat = "count") +
facet_grid(Experiment~phase_name) +
ggthemes::theme_few() +
labs(x = "Number of looks", y = "Count") +
scale_fill_discrete(name="Trial type")
